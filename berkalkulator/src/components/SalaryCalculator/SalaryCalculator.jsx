import { Tabs, TabsContent} from "@/components/ui/tabs";
const SalaryCalculator = ({avtiveUser}) => {
  return (
    <div>
      <h1>{avtiveUser.nev} bérének kiszámítása</h1>
      
      
    </div>
  );
};

export default SalaryCalculator;
